import {account, cart, discount, graph, home, item1, item2, item3, item4, menu, message, notification} from "../assets";

const buttonImgArr = [menu, account, graph, message, discount, notification, cart, home ]

const imgs = [item1, item2, item3, item4, item1, item2, item3, item4, item1, item2, item3, item4, item1]

const selectOptions = ['A-Z', 'Z-A', 'Price', 'Default']

export {buttonImgArr, imgs, selectOptions}